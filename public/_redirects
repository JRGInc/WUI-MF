# SPA fallback: serve index.html for client-side React Router routes
# (e.g. /report/:token, /assessment/new, /assessment/:id/edit) so hard
# refreshes and direct links don't 404 on Cloudflare Pages.
/*  /index.html  200
